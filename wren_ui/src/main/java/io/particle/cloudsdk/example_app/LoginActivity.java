package io.particle.cloudsdk.example_app;import android.content.Intent;import android.os.Bundle;import android.support.v7.app.AppCompatActivity;import android.util.Log;import android.view.View;import android.widget.EditText;import android.widget.Toast;import java.io.IOException;import io.particle.android.sdk.accountsetup.CreateAccountActivity;import io.particle.android.sdk.accountsetup.PasswordResetActivity;import io.particle.android.sdk.cloud.ParticleCloud;import io.particle.android.sdk.cloud.ParticleCloudException;import io.particle.android.sdk.cloud.ParticleDevice;import io.particle.android.sdk.utils.Async;import io.particle.android.sdk.utils.Toaster;public class LoginActivity extends AppCompatActivity {    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_login);        hideSystemUI();        getSupportActionBar().hide();        findViewById(R.id.login_button).setOnClickListener(                v -> {                    final String email = ((EditText) findViewById(R.id.email)).getText()                            .toString();                    final String password = ((EditText) findViewById(R.id.password)).getText()                            .toString();                    Async.executeAsync(ParticleCloud.get(v.getContext()),                            new Async.ApiWork<ParticleCloud, Object>() {                                private ParticleDevice mDevice;                                @Override                                public Object callApi(ParticleCloud sparkCloud)                                        throws ParticleCloudException, IOException {                                    // TODO: remove user name and password                                    String myemail = "adm6006s@gmail.com";                                    String mypassword = "testtest6006";                                    sparkCloud.logIn(myemail, mypassword);                                    // sparkCloud.logIn(email, password);                                    sparkCloud.getDevices();                                    return -1;                                }                                @Override                                public void onSuccess(Object value) {                                    // Toaster.l(LoginActivity.this, "Logged in");                                    Intent intent = ValueActivity                                            .buildIntent(LoginActivity.this, 123, null);                                    startActivity(intent);                                }                                @Override                                public void onFailure(ParticleCloudException e) {                                    Toaster.l(LoginActivity.this, e.getBestMessage());                                    e.printStackTrace();                                    Log.d("info", e.getBestMessage());                                }                            });                }        );        findViewById(R.id.textview_no_account).setOnClickListener(view -> {            // 12 Feb 2016: It's possible the user can hit "I don't have an account" -> "I already            // have an account" -> "I forgot my password" and com across a bug.            // https://github.com/spark/photon-tinker-android/issues/10            //            // The quick fix was to change the customization file as follows:            //            // OLD: <string name="forgot_password_uri">https://www.login.particle.io/forgot</string>            //            // NEW: <string name="forgot_password_uri">https://login.particle.io/forgot</string>            //            // An alternative way would be to set up an async task and use the ParticleCloud class            // to avoid using their activity.            //            // ParticleCloud.signUpAndLogInWithCustomer(email, user, org);            // OR            // ParticleCloud.signUpWithUser(String user, String password);            Intent intent = new Intent(this, CreateAccountActivity.class);            startActivity(intent);        });        findViewById(R.id.textview_no_password).setOnClickListener(view -> {            Intent intent = new Intent(this, PasswordResetActivity.class);            startActivity(intent);        });    }    // This snippet hides the system bars.    private void hideSystemUI() {        // Set the IMMERSIVE flag.        // Set the content to appear under the system bars so that the content        // doesn't resize when the system bars hide and show.        getWindow().getDecorView().setSystemUiVisibility(                View.SYSTEM_UI_FLAG_LAYOUT_STABLE                        | View.SYSTEM_UI_FLAG_LAYOUT_HIDE_NAVIGATION                        | View.SYSTEM_UI_FLAG_LAYOUT_FULLSCREEN                        | View.SYSTEM_UI_FLAG_HIDE_NAVIGATION // hide nav bar                        | View.SYSTEM_UI_FLAG_FULLSCREEN // hide status bar                        | View.SYSTEM_UI_FLAG_IMMERSIVE_STICKY);    }    // This snippet shows the system bars. It does this by removing all the flags    // except for the ones that make the content appear under the system bars.    private void showSystemUI() {        getWindow().getDecorView().setSystemUiVisibility(                View.SYSTEM_UI_FLAG_LAYOUT_STABLE                        | View.SYSTEM_UI_FLAG_LAYOUT_HIDE_NAVIGATION                        | View.SYSTEM_UI_FLAG_LAYOUT_FULLSCREEN);    }    public void noPassword(View view) {        Toast.makeText(getBaseContext(), "No Password", Toast.LENGTH_SHORT).show();    }    public void noAccount(View view) {        Toast.makeText(getBaseContext(), "No Account", Toast.LENGTH_SHORT).show();    }}